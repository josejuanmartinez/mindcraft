transformers==4.35.2
sentence_transformers==2.2.2
autoawq==0.1.7
requests~=2.31.0
torch==2.1.2

# VECTOR STORES OPTIONS:
# For ChromaDB:
# chromadb==0.4.18

# TEXT SPLITTING OPTIONS
# For token-based splitting of texts to be stored in the vector store:
# tiktoken==0.5.1
# For sentence-based splitting of texts:
# spacy==3.7.0
